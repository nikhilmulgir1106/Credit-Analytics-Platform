# CreditPulse — Python dependencies (local-first, DuckDB-primary)
# Top-level deps only; pip resolves compatible transitive versions.
# Target: Python 3.12 on macOS (M2, 8 GB RAM).

# --- Warehouse + transformation ---
duckdb>=1.1
dbt-duckdb>=1.9          # pulls in dbt-core

# --- Data quality ---
great-expectations>=1.3

# --- ML ---
scikit-learn>=1.5
xgboost>=2.1

# --- Dashboard ---
streamlit>=1.40

# --- Data handling ---
pandas>=2.2

# --- Distributed-processing demo (sampled data only) ---
pyspark>=3.5

# --- Synthetic data generation ---
faker>=33.0

# --- Source readers ---
xlrd>=2.0                # read the UCI legacy .xls source file
