# Python
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Environment / secrets
.env

# Data — raw/processed datasets are never committed (public/synthetic, re-downloadable)
data/

# DuckDB warehouse file(s)
warehouse/
*.duckdb
*.duckdb.wal

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Great Expectations local artifacts
quality/great_expectations/uncommitted/

# ML — trained model binaries + metrics (reproducible from train.py)
ml/artifacts/

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json
